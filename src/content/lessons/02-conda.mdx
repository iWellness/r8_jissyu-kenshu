---
slug: conda
order: 2
title: conda環境の設定
description: Miniforgeを使ってcondaを導入し、環境を管理する準備をします。
objectives:
  - Miniforgeのインストーラを取得して実行できる
  - condaを初期化してバージョンを確認できる
prerequisites:
  - SSH接続済み
environment: SSH接続先のLinux
---

import Callout from '../../components/Callout.astro';
import CommandBlock from '../../components/CommandBlock.astro';
import Figure from '../../components/Figure.astro';
import curlInstaller from '../../snippets/02-conda/01-curl-installer.txt?raw';
import wgetInstaller from '../../snippets/02-conda/02-wget-installer.txt?raw';
import runInstaller from '../../snippets/02-conda/03-run-installer.txt?raw';
import initConda from '../../snippets/02-conda/04-init-conda.txt?raw';
import miniforgeImage from '../../assets/02_miniforge-download.png';

## condaとは

condaは、実行環境とパッケージをまとめて管理するツールです。`pip`と異なり、Python以外のパッケージも扱えます。

## インストール（Linux）

### 1. 公式サイトにアクセス

[Miniforge - GitHub](https://github.com/conda-forge/miniforge) にアクセスします。

### 2. Linux用のインストーラを選ぶ

`Unix-like platforms (macOS & Linux)` の項目を見つけます。

<Figure src={miniforgeImage} alt="Miniforgeのダウンロード一覧にあるUnix-like platformsの項目" caption="Unix-like platforms（macOS & Linux）のダウンロード項目" />

### 3. インストーラをダウンロード

`curl`と`wget`のどちらか一方だけを実行します。

<CommandBlock id="conda-download-curl" code={curlInstaller} language="bash" kind="command" context="ターミナル" title="curlでダウンロード" />

<CommandBlock id="conda-download-wget" code={wgetInstaller} language="bash" kind="command" context="ターミナル" title="wgetでダウンロード" />

### 4. インストーラを実行

<CommandBlock id="conda-run-installer" code={runInstaller} language="bash" kind="command" context="ターミナル" title="Miniforgeのインストール" />

質問が表示されたら、次のように進めます。

- Enterでページを進める。
- `(END)` と表示されたら、`q` を入力して終了する。
- `accept the license terms? [yes|no]` と聞かれたら、`yes` を入力してEnterを押す。
- `You can undo this by running conda init --reverse $SHELL? [yes|no]` と聞かれたら、`no` を入力してEnterを押す。

<Callout type="warning" title="インストーラの質問を確認">
  表示される質問文や初期化先は、インストーラの版やシェルによって変わる場合があります。内容を読んでから入力してください。
</Callout>

### 5. condaの初期化と確認

<CommandBlock id="conda-initialize-and-check" code={initConda} language="bash" kind="command" context="ターミナル" title="condaを初期化してバージョンを確認" />

このコマンドでは、講座環境の `/home/r8user/miniforge3` にあるcondaを使って、zsh用の初期化処理を読み込みます。
